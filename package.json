{
  "name": "inventory-system-root",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev:client": "npm run dev --workspace=client",
    "dev:server": "npm start --workspace=server",
    "start": "npx concurrently \"npm run dev:client\" \"npm run dev:server\""
  }
}
